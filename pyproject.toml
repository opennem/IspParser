[project]
name = "ispparser"
version = "0.1.0"

dependencies = [
    "pandas",
    "numpy",
    "pytz",
    "python-calamine",
    "openpyxl",
]

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
